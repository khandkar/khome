#! /bin/bash

set -e

declare -a ZONES=(
    US/Pacific
    US/Eastern
    Europe/Moscow
    #Europe/Rome
    Poland
    Asia/Calcutta
    NZ
)

for tz in ${ZONES[*]}
do
    printf '%s ' "$tz"
    TZ="$tz" date '+%a %b %d %H:%M:%S'
done \
| column -t
